# models — Saved Policy Checkpoints

Trained policy files (.zip for Stable-Baselines3, .onnx for Pi deployment).
Large binary files are gitignored by default; only deliberately chosen
checkpoints should be committed.
