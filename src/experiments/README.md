# src/experiments — Ablation Study & Data Collection

Contains scripts that run the three experimental conditions (no DR, DR only,
DR + system ID) and the per-factor ablation. Saves metrics (distance, speed,
fall rate, IMU tilt variance) to data/. **Phase 5.**
